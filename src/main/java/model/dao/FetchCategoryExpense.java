package model.dao;

import java.sql.ResultSet;

public class FetchCategoryExpense {
//	public static String query="select sum(amount) category_expenses,name as category_name from(select c.id,e.category_id,e.amount,e.u_id,c.name from category c \r\n"
//			+ "inner join expense e on c.id=e.category_id) as x group by id";
//	public static ResultSet fetchExpense() {
//		
//		
//	}
}
